# Module Topology

No modules are declared for this root-only Harness installation.

When a multi-module installation is explicitly initialized, Niuma renders this document from the project-maintained module registry. It routes an Agent from root policy to only the affected module supplements; it never replaces current code, configuration, build definitions, or command output as the source of task facts.
