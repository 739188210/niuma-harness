# <栈名> 适配包 · 模板

> 复制本文件为 `<栈>.md`(如 `go.md`、`rust.md`),按下列结构填写。
> 适配包只在该项目 `PROJECT-CONTEXT.md` 未记录约定时提供**行业默认**,或帮助识别栈特征——**不替代项目实际约定**。

## 0. 如何使用 / 约定优先级

- 约定来源优先级遵循 `references/项目上下文指南.md` 第 3 节;本适配包只提供低于项目事实的行业默认规则。
- 本包列「常见选项」,不指定唯一答案;具体用哪个由当前项目事实和经校验的 `PROJECT-CONTEXT.md` 确认。
- 若本项目有偏离,按 `references/项目上下文指南.md` 第 3 节处理,并在需要时记录为待写回 Project Context 候选。
- 进入实施/验证前,若项目主栈为本栈,应加载并参考本文件。

---

## 1. 识别信号

判断项目是否属于本栈的依据:

- 构建文件 / 包管理文件:
- 依赖特征(关键依赖名):
- 目录特征:

---

## 2. 构建与校验命令

| 用途 | 常见命令(用项目实际的) |
|---|---|
| 安装依赖 | |
| 构建 / 编译 | |
| 运行测试 | |
| 类型检查 / lint | |
| 覆盖率 | |

---

## 3. 分层与命名

- 常见分层:
- 命名约定:
- 文件后缀:

> 以 `PROJECT-CONTEXT.md` 记录的实际分层与命名为准。

---

## 4. 测试

- 框架:
- Mock 工具:
- 测试目录约定:
- 覆盖率工具:

---

## 5. 数据或状态访问

- 数据访问 / 状态管理方式:

---

## 6. 典型红线与陷阱

- 

---

## 7. 一句话总结

- 
