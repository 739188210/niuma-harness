---
name: dev-guide-lite
description: 轻量化开发指导。用于功能开发、Bug 修复、页面改动和小型重构；默认采用实用 TDD；涉及页面、组件、样式或交互改动时，必须尝试用 Playwright 操控浏览器验证真实效果。
when_to_use: 用户要求实现功能、修 Bug、改页面、调整交互、修改组件、优化小段代码或做小型重构，且不需要完整需求/设计文档流程时使用。
---

# dev-guide-lite · 轻量 TDD 开发指导

`dev-guide-lite` 是一套独立的轻量开发流程，用于日常功能开发、Bug 修复、页面改动和小型重构。

它的目标不是产出需求文档或设计文档，而是让开发过程保持：

- 目标清楚
- 改动克制
- TDD 优先
- 验证真实
- 页面改动必须浏览器验收

---

## 核心原则

1. **轻量优先**
   - 不生成固定多阶段文档。
   - 不为了流程完整而写长篇说明。
   - 默认只输出短计划和验证结果。

2. **实用 TDD**
   - 优先先写测试或复现步骤。
   - Bug 修复优先先复现。
   - 项目不具备自动化测试条件时，用可执行验证步骤替代，但必须说明原因。

3. **最小实现**
   - 只改和本次目标直接相关的代码。
   - 不顺手重构。
   - 不引入无关依赖。
   - 不扩大需求范围。

4. **真实验证**
   - 代码写完不算完成。
   - 测试、构建、类型检查、浏览器验证通过后，才算完成。
   - 不允许只用“代码看起来没问题”作为验证结论。

5. **页面改动必须尝试 Playwright**
   - 只要影响用户可见页面、组件、样式、交互或路由，就必须尝试用 Playwright 操控浏览器验证真实效果。
   - 如果因为登录、环境、服务、账号等原因阻塞，必须明确说明阻塞点，并请求用户配合后继续验证。

---

## 1. 适用范围

适用于：

- 功能开发
- Bug 修复
- 页面改动
- 组件调整
- 表单交互
- 样式修改
- 小型重构
- 单个接口或单条调用链调整
- 局部性能或体验优化

不适用于：

- 大型架构设计
- 跨系统重构
- 数据迁移方案
- 权限体系重建
- 支付、结算、资金链路重构
- 多服务协同改造
- 需要完整 PRD / 技术设计 / 实施计划的复杂需求

遇到不适用场景时，不要强行套用本 skill；应先提醒用户需要更重的设计或单独确认方案。

---

## 2. 任务分级

按风险和影响范围分为三类。

### quick

适合：

- 文案修改
- 样式微调
- 常量、枚举、配置小改
- 单文件小 Bug
- 明确且低风险的小改动

流程：

```text
确认目标 → 修改 → 最小验证 → 汇报
```

### normal

适合：

- 普通功能开发
- 明确 Bug 修复
- 单个接口调整
- 单个页面或组件改动
- 表单、按钮、弹窗、交互调整
- 小型重构

流程：

```text
目标 → 测试/复现 → 实现 → 自动测试 → Playwright 如适用 → 汇报
```

### careful

适合：

- 权限、用户数据、支付、订单、导出、批量操作
- 公共组件、公共 API、公共工具修改
- 影响多个页面或多个调用方
- 虽然代码量小，但失败代价高的改动
- 不可逆或难回滚的操作

流程：

```text
简短方案 + 风险说明 → 必要时等用户确认 → 测试/复现 → 实现 → 验证 → Review → 汇报
```

`careful` 不等于重文档，但必须在动手前说明风险。

---

## 3. 开发前最小对齐

开始前先给出简短计划。

默认格式：

```markdown
目标：...
级别：quick / normal / careful
验证方式：...
页面验证：需要 / 不需要，原因：...
计划：
1. ...
2. ...
3. ...
```

如果需求不清楚，先问用户，不要猜。

必须澄清的情况：

- 目标不明确
- 成功标准不明确
- 页面路径或入口不明确
- 需要登录但没有测试账号或登录方式
- 涉及权限、数据、支付、批量操作等高风险行为
- 有多个合理实现方向，且选择会影响用户体验或架构

---

## 4. 实用 TDD

默认采用实用 TDD，而不是机械 TDD。

### Bug 修复

优先顺序：

1. 写一个能复现问题的自动化测试。
2. 如果自动化测试成本过高，记录可执行复现步骤。
3. 确认复现的问题与用户描述一致。
4. 写最小修复。
5. 重新运行测试或复现步骤，确认问题消失。

### 新功能

优先顺序：

1. 写最小行为测试或验收用例。
2. 实现刚好满足测试的功能。
3. 补充边界测试。
4. 验证用户可见行为。

### 页面改动

页面改动可以把 Playwright 操作步骤作为验收用例。

例如：

```text
1. 打开页面。
2. 找到目标按钮。
3. 点击按钮。
4. 验证弹窗出现。
5. 填写表单。
6. 提交后验证成功提示或页面状态。
```

### 允许不用自动化测试的情况

以下情况可以不新增自动化测试，但必须说明原因并给出验证方式：

- 项目没有测试框架。
- 测试环境缺失。
- 改动仅为文案或极小样式调整。
- 自动化测试成本明显高于改动价值。
- 当前任务更适合用 Playwright 验收。

禁止：

- 不测试也不说明原因。
- 用“应该没问题”代替验证。
- 为了过测试而改错测试语义。

---

## 5. 页面改动 Playwright 规则

只要本次改动影响以下任一项，必须尝试 Playwright：

- 页面
- 路由
- layout
- 组件
- 样式
- 表单
- 按钮
- 弹窗
- 菜单
- 导航
- 加载态
- 错误态
- 空状态
- 权限展示
- 响应式布局
- 前端交互逻辑

### Playwright 最小验证项

至少检查：

1. 页面能打开，没有白屏。
2. 控制台没有新增 `error`。
3. 目标元素可见。
4. 目标交互可以完成。
5. 页面结果符合预期。
6. 涉及视觉、布局、样式时，查看页面或截图。
7. 涉及响应式时，至少检查一个桌面宽度和一个移动宽度。

### 登录阻塞处理

如果页面需要登录：

1. 不要伪造验证结果。
2. 先尝试判断是否已有登录态。
3. 如果没有登录态，提醒用户在浏览器中手动登录。
4. 如果需要账号，向用户请求测试账号或测试环境账号。
5. 不要求用户提供生产账号或敏感密码。
6. 用户完成登录后，继续 Playwright 验证。

汇报格式：

```markdown
Playwright：未完成
阻塞原因：需要登录 / 缺少测试账号 / 缺少环境变量 / 服务无法启动
已请求用户：...
待继续验证：...
```

### 环境阻塞处理

如果因为环境问题无法验证：

- 明确失败命令。
- 摘要关键错误。
- 判断是依赖缺失、环境变量缺失、后端不可用、端口冲突还是构建失败。
- 能最小修复则修复。
- 不能修复则向用户说明需要什么输入。

禁止：

- 跳过浏览器验证但写成已验证。
- 只看代码不打开页面。
- 服务没启动就声称页面正常。

---

## 6. 服务启动规则

页面验证前必须确认服务状态。

优先级：

1. 用户提供的 URL。
2. 已经运行的本地服务。
3. 项目脚本中的启动命令。
4. README、文档、Makefile、脚本文件中的启动方式。
5. 询问用户。

如果服务未启动，应使用项目已有命令启动。

常见来源：

- `package.json`
- `pnpm-lock.yaml`
- `yarn.lock`
- `package-lock.json`
- `bun.lockb`
- `README.md`
- `Makefile`
- `docker-compose.yml`
- 项目本地脚本目录

不要在不知道项目启动方式时乱试命令。

启动后：

1. 等待服务可访问。
2. 打开目标页面。
3. 执行 Playwright 验证。
4. 收集控制台错误和关键页面状态。
5. 完成后汇报验证结果。

如果启动失败：

```markdown
服务启动：失败
命令：...
错误摘要：...
判断原因：...
下一步：已修复 / 需要用户提供 ...
```

---

## 7. 实现约束

开发时遵守以下约束：

- 匹配项目现有风格。
- 优先复用已有组件、工具、接口和模式。
- 不因为一个局部需求引入新架构。
- 不新增无关抽象。
- 不扩大改动范围。
- 不顺手修无关问题。
- 不删除无关代码。
- 不改变未被要求改变的行为。
- 不引入新依赖，除非已有方案明显不足，并说明原因。
- 修改公共代码时，检查所有调用方影响。
- 修改用户输入、接口、权限、数据、文件、外部调用时，显式处理错误和边界。

---

## 8. 高风险暂停点

遇到以下情况必须暂停并向用户确认，不能静默推进：

- 修改权限、鉴权、授权逻辑。
- 修改支付、订单、资金、结算相关逻辑。
- 修改用户数据、隐私数据、敏感字段。
- 修改数据库结构或迁移数据。
- 修改公共 API 契约。
- 修改公共组件并影响多个页面。
- 修改登录、Session、Token、Cookie、SSO。
- 修改批量操作、导入、导出、删除。
- 执行不可逆操作。
- 需要生产账号、生产数据或敏感凭据。
- 发现现有实现可能有安全问题或数据越权问题。

提问时必须给出：

```markdown
风险：...
推荐方案：...
影响范围：...
回滚方式：...
需要你确认：...
```

---

## 9. Review 与质量检查

代码改完后，根据项目规则执行必要检查。

常见检查：

- 单元测试
- 集成测试
- 类型检查
- lint
- build
- Playwright 页面验证
- code review
- security review

如果检查失败：

1. 不要汇报完成。
2. 摘要失败原因。
3. 优先做最小修复。
4. 修复后重新验证。
5. 如果无法修复，说明阻塞点。

---

## 10. 完成汇报格式

完成后只汇报关键结果，不写长文档。

默认格式：

```markdown
已完成：...
关键改动：...
验证结果：
- 测试：...
- 类型检查 / 构建：...
- Playwright：...
风险 / 未覆盖：...
```

如果没有运行某项验证，必须说明原因：

```markdown
未运行：...
原因：...
替代验证：...
```

如果 Playwright 被阻塞，必须说明：

```markdown
Playwright：未完成
阻塞原因：...
需要用户提供：...
登录或环境补齐后可继续验证：是 / 否
```

---

## 11. 隔离工作区模式

当用户要求并行执行多个任务、使用 worktree、避免任务互相污染，或当前工作区已有未提交改动时，启用隔离工作区模式。

隔离工作区模式用于为每个任务创建独立本地分支和独立工作目录，避免多个任务在同一工作目录中互相覆盖文件。

### 触发条件

- 用户明确说“并行做多个任务 / 使用 worktree / 隔离开发”。
- 同一轮需要执行多个相互独立的开发任务。
- 多个 agent 或多个 CLI 工具会同时修改代码。
- 当前工作区已有未提交改动，继续开发可能污染现场。
- 用户希望每个任务独立分支、独立验证、独立提交。

### 执行原则

1. 每个任务对应一个独立 Git 分支。
2. 每个任务对应一个独立 worktree 目录。
3. 一个 agent / CLI 会话只在一个 worktree 中工作。
4. worktree 只解决工作目录隔离，不保证最终合并无冲突。
5. 修改共享文件、公共组件、schema、migration、lockfile 时要额外提示冲突风险。
6. 启用前必须确认目标目录是 Git 仓库；如果不是 Git 仓库，先询问用户目标仓库路径。

### 工具适配

- Claude Code：优先使用 Claude Code 原生 worktree 能力进入隔离目录。
- Codex CLI：使用 Git 原生 `git worktree` 创建独立目录和分支，再让 Codex 在该目录运行。
- 其他工具：使用 Git 原生 worktree，并把工具工作目录指向该 worktree。

### 默认不做

除非用户明确要求，否则不要自动：

- push 到远程分支
- 创建 GitLab Merge Request
- 删除 worktree
- 删除本地分支
- 丢弃未提交改动

### 完成后汇报

```markdown
隔离工作区：已启用 / 未启用
分支：...
worktree 路径：...
基础分支：...
验证结果：...
后续操作：是否需要 push / 创建 MR / 清理 worktree
```

---

## 12. 禁止行为

禁止：

- 未确认高风险决策就修改。
- 页面改动后不打开浏览器验证。
- 服务没启动却声称验证通过。
- 登录受阻时直接跳过验证且不说明。
- 测试失败时说完成。
- 构建失败时说完成。
- 为了通过测试修改测试语义。
- 顺手重构无关代码。
- 引入无关依赖。
- 删除无关文件。
- 伪造测试、构建、浏览器验证结果。
